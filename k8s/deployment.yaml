apiVersion: v1
kind: Pod
metadata:
  name: server-pod-userservice
  labels:
    name: server-pod-userservice
    app: cluster-1

spec:
  containers:
    - name: server-pod-userservice
      image: pinto97/user_service
      ports:
        - containerPort: 8080
